CREATE TABLE pages (
    pubscheduler_publication_type varchar(255) DEFAULT 'default' NOT NULL,
);

CREATE TABLE tt_content (
    pubscheduler_publication_type varchar(255) DEFAULT 'default' NOT NULL,
);